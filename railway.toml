[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/gunicorn leadclaw.web:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/login"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
